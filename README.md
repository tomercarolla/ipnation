# IPNation

## IP to Country Engine

A simple React + TypeScript application where the user enters an IP address and receives the corresponding country flag and timezone.

## 📦 Install

After cloning the repository, install dependencies:

```bash
npm install
```

## ▶️ Run the app

```bash
npm run dev
```

## 🧪 Run tests

```bash
npm test
```